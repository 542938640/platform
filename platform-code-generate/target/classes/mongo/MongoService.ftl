package ${config.package_name}.${config.module_name}.service;

import ${config.package_name}.${config.module_name}.entity.${config.class_name}Entity;
import ${config.package_name}.service.BaseMongoService;

/**
 *<p>Title:${config.class_name}Service.java</p>
 *<p>Description:${mongo.name} Service</p>
 *@author ${config.author}
 *@version 1.0
 *@Automatically generate by Coder in ${config.generator_time}
 */
public interface ${config.class_name}Service extends BaseMongoService<${config.class_name}Entity> {
	
}