package ${config.package_name}.${config.module_name}.mapper;

import ${config.package_name}.mapper.IBaseMapper;
import ${config.package_name}.${config.module_name}.entity.${config.class_name}Entity;

/**
 *<p>Title:${config.class_name}Mapper.java</p>
 *<p>Description:${tableInfo.remarks} Mapper</p>
 *@author ${config.author}
 *@version 1.0
 *@Automatically generate by Coder in ${config.generator_time}
 */
public interface ${config.class_name}Mapper extends IBaseMapper<${config.class_name}Entity> {

}